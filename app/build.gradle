plugins {
    id 'com.android.application'
}

android {
    namespace 'com.thinking.locker'
    compileSdk 34

    defaultConfig {
        applicationId "com.thinking.locker"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
    
    buildTypes {
        debug {
            // ini penting biar task assembleDebug ada
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}
